version: '3.8'

services:
  user-service:
    build:
      context: .
      dockerfile: ./user-service/app/Dockerfile
    container_name: user-service
    ports:
      - "8001:8001"
  product-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: product-service
    ports:
      - "8002:8002"
  order-service:
    build:
      context: .
      dockerfile: ./order-service/app/Dockerfile
    container_name: order-service
    ports:
      - "8003:8003"

  api-gateway:
    image: nginx
    container_name: api-gateway
    volumes:
      - ./api-gateway/nginx.conf:/etc/nginx/nginx.cong
    ports:
      - "8000:80"
    depends_on:
      - user-service
      - product-service
      - order-service

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    container_name: front-end
    ports:
      - 3000:80
    depends_on:
      - api-gateway
